# Copy this file to .env and set your values.
# Do not commit .env (it is in .gitignore).

# PostgreSQL
POSTGRES_HOST=138.226.240.119
POSTGRES_PORT=5434
POSTGRES_DB=test_db
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_12311

# Optional: connection pool size for API (0 = no pool)
POSTGRES_POOL_SIZE=0

# Redis (optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Airflow (set by setup_airflow.py / start_airflow.bat)
# AIRFLOW_HOME=./airflow_home
